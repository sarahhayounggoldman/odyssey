<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Profile</title>
    <!-- load local stylesheet (css) -->
    <link rel="stylesheet" href="/styles.css" />
    <!-- loading jquery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>
    <%- include("partials/navbar.ejs") %>
    <%- include("partials/flashes.ejs") %>
    <h1>Welcome!</h1>
    <h1>YOU HAVE SUCCESSFULLY LOGGED IN AS <%= username %> AND THIS IS YOUR PROFILE PAGE</h1>

    <h2>@<%= username %></h2>

    <p>bio goes here</p>

    <p># of followers goes here</p>
    <p># following goes here</p>

    <p>
       
          <button class="profile-button">Logout</button>
    </p>

    <body>
        <form action="/profile" method="POST">
        <% if (posts.length > 0) { %>
            <% posts.forEach(post => { %>
                <%- include('post', { post: post }) %>
            <% }); %>
        <% } else { %>
            <p>No posts yet.</p>
        <% } %>
        </form>
    </body>

    <footer class="footer">
        <p>Copyright © The Odyssey Team 2024 all rights reserved.</p>
    </footer>

    <!-- load local js -->
    <script src="/main.js"></script>
<!-- uid script -->
</body>
</html>
